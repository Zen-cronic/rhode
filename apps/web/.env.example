# Empty uses the local Vite /api proxy. Production must configure API origin or reverse proxy.
VITE_API_URL=
# Only set this for the loopback synthetic environment.
VITE_AUTH_MODE=local-demo
VITE_GOOGLE_MAPS_KEY=
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
