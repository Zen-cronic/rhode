import React from 'react';
import {Text} from 'react-native';
export function TripMap(_props:unknown){return <Text>Native map is available in Android/iOS builds. Use the dispatcher web application for browser maps.</Text>;}
