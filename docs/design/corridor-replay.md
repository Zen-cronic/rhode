# Corridor replay — implementation brief

Status: FUNCTIONALLY_ACCEPTED; VISUAL_NEEDS_REVISION (8.0/10 at critique cap). Packet `PB-RS-3D-CORRIDOR-20260912`, September 12, 2026. Conservative preparation target is September 13 at noon EDT; the official noon/2 PM conflict remains unresolved. The operator explicitly approved 3D implementation and later approved retaining the third Precision transport design.

The dispatcher should be able to open one recorded synthetic trip and understand the long dock wait, the start of motion, observed speed and distance, and the current route epoch. This is a presentation-time instrument over acknowledged history. Its controls must never resume, advance, reset, approve, invoice, or change scenario time.

Use the accepted Precision transport direction: graphite structure, warm ivory work surface, signal-orange route, Manrope hierarchy and IBM Plex Mono evidence. Ground the composition in the inspected Grab navigation hierarchy, Felt map-first workspace, Slate physical-object restraint, and Pallet's decision/evidence juxtaposition recorded in `docs/design/references.md`. No third-party meshes or images are copied. The renderer uses procedural geometry and the source GeoJSON only.

Composition: a wide dark corridor stage within Simulation studio, with the route as a raised ivory/graphite ribbon and the recorded truck position in orange. Use a shallow perspective or orthographic camera so the full route reads immediately. The DOM header owns route name, synthetic/evidence labels, observation timestamp, phase, speed and odometer. A tactile scrub rail and play/pause/restart controls operate only a local view cursor. Include milestone buttons for origin, movement start and latest observation. A source disclosure shows the exact sample ID, route key and recording snapshot. Current HOS budgets, detention amount and operational controls stay outside this historical surface.

Playback may sample frames for rendering efficiency, but the selected detail must refer to the exact acknowledged source event. Speed describes the preceding interval; phase and duty describe the observation instant. Route geometry is selected by each event's `route_key`; an adopted-route boundary must not paint earlier samples onto the latest route. No interpolation may cut across roads. If animation interpolates, interpolate only between adjacent acknowledged positions in the same route epoch and snap at epoch changes. Reduced motion disables automatic playback and camera easing. The scene uses `frameloop="demand"`, DPR at most 1.5 and a finite render loop while playing.

Loading shows page progress. A stale snapshot conflict discards the partial recording and offers a fresh reload; it never combines pages. Offline after a completed load keeps the frozen historical recording and labels it saved. Offline before completion, API error, empty recording, WebGL failure and manual diagram mode remain readable. Keyboard controls include Space for play/pause, Home/End for first/latest, and Arrow keys for exact-event stepping when the timeline has focus. Native HTML controls provide every interaction available through the scene.

Implementation ownership: the designer worker owns new `apps/web/src/CorridorReplay.tsx`, `apps/web/src/CorridorScene.tsx` and `apps/web/src/corridor-replay.css`. The coordinator owns data parsing/model tests, integration in `SimulatorControls.tsx`, verification scripts, screenshots, evidence, commits and shared checkpoints. Other agents may edit the repository; preserve their work and do not modify existing dock files unless the coordinator explicitly expands ownership.

Acceptance: exact 10,201-event recording loads across bounded pages; selected source identity and route epoch remain exact; view controls make zero operational POSTs and do not alter the inventory state; built desktop and 390 px layouts have no horizontal overflow; reduced-motion, keyboard, loading/error/empty/offline and WebGL/diagram fallbacks work; lazy scene lifecycle and bundle separation are measured; actual screenshots are inspected. Run focused model/component checks, web/root TypeScript, production build and a browser verifier. Maximum two repair attempts per blocker, 30-minute packet, $0 incremental spend. Keep only if source parity, read-only behavior, built-browser proof and visual inspection pass.

Visual critique uses the fixed `skills/designer/references/critic-prompt.md` with fresh screenshot-only contexts. This is a new surface, so the corridor packet has its own initial assessment plus at most two reassessments and a subjective 9/10 ambition. A score does not prove behavior. If the cap is exhausted, keep a functionally passing surface as `NEEDS_REVISION`; do not invent visual verification.
