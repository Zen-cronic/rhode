# Dock recovery rehearsal — implementation brief

User approved 3D implementation September12 after native corrections were committed. Preserve the third Precision transport design. Audience: dispatcher and live demonstration audience. Primary task: understand how a dock wait consumes on-duty headroom, exposes detention, and changes the next assignment decision.

The first surface is an explicitly illustrative procedural dock miniature inside the local simulation studio. The independent simulator and operational API remain authoritative. Use acknowledged sample phase/identity; keep snapshot times readable. No historical scrubber, independent clock, local billing calculation, surveyed geometry claim or automatic assignment. Controls select an object or camera; existing reviewed commands remain outside the canvas.

Grounding: existing `precision-transport.md` and inspected `references.md`; operator-supplied winner studies summarized in suite `research/simulation-3d-direction-2026-09-12.md`. Procedural event consumers and repeatable cameras transfer from feetball/Auctopus; no third-party meshes or imagery are copied. Existing source-verified S06 dock fixture supplies realistic evidence. Full corridor/replay/comparison remain subsequent packets.

Composition: a warm ivory warehouse/truck miniature with graphite roof, restrained orange selected path and fence; editorial heading and three camera buttons; current HOS and provisional detention in DOM below; source IDs in an expandable inspector. Manrope headings, IBM Plex Mono source labels. Desktop420px/narrow320px scene. No infinite animation. Finite camera easing honors reduced motion, demand render loop and DPR≤1.5. Static diagram and readable evidence survive WebGL loss. Mouse selection has DOM-button alternatives.

Coordinator owns source projection, container/CSS, integration, dependencies, verification and checkpoints. Independent designer owns DockScene.tsx and dock-scene.css. NATIVE_PARALLEL implementation; fresh visual critic receives screenshots only. Initial critique +at most2reassessments; subjective target9/10; score does not establish functionality. Product packet budget30min from19:18UTC, max2repairs per blocker.

Dependency basis: official R3F installation documentation pairs major9 with React19; installed peer range excludes19.3, so web React/ReactDOM pinned19.1.0, Fiber9.7.0, Three/types0.186.0. https://raw.githubusercontent.com/pmndrs/react-three-fiber/master/docs/getting-started/installation.mdx and https://raw.githubusercontent.com/pmndrs/react-three-fiber/master/docs/API/canvas.mdx verified September12. No new cloud resource or model-generation cost; renderer runs in the client. Existing non-renderer dependency audit findings are retained for a separate maintenance review; no forced framework upgrade.

Proof: pure source binding/scope/held/unknown tests; built Vite scene at1440 and390width; current S06 amounts/HOS, three cameras, selection, no overflow, offline dated snapshot, manual diagram and context-loss fallback, held-response fixture; unchanged operational records/event count. GPU timing and physical native3D are not claimed by browser screenshots. All assets are code-authored primitives; package licenses retained in lock/dependencies. Rebuild and recapture after visual changes.
