# Current 20-scenario acceptance map

Reviewed September 12, 2026 against the RoadStar research matrix. “Accepted” means the exact bounded behavior in that row has current retained evidence. It does not expand a local, responsive-browser or emulator check into a physical-device, certified ELD, legal-clearance, production-capacity or measured-savings claim.

| ID | Status | Current evidence and proven behavior | Boundary |
|---|---|---|---|
| S01 ordinary trip | Accepted | [`ordinary-trip.md`](ordinary-trip.md): the same accepted RS-1042 assignment appears in the 390px driver manifest, complete-trip mileage, breadcrumb view and completed destination-visit billing. PostgreSQL retains 7,801 unique observations, 0–68.6 km/h and 4.436 km. | Local responsive browser over synthetic London data. Assignment acceptance, completed visit and stop-completion remain distinct. |
| S02 sustained slowdown | Accepted | [`simulator-slowdowns.md`](simulator-slowdowns.md) and [`3d-slowdown-comparison.md`](3d-slowdown-comparison.md): identical routed branches emit 2,401 samples each; the 30-minute 10% slowdown travels 13.316 km versus 40.973 km, consumes the same 40 driving minutes and moves forecast completion 26m46s later. Historical speeds and exact IDs are visible; the matched 3D/diagram replay exposes the 27.657 km progress gap at the shared cursor. | Forty-minute controlled comparison, not measured traffic accuracy, a physical-GPU benchmark or a completed route. |
| S03 feasible closure | Accepted | [`simulator-adoption.md`](simulator-adoption.md), [`simulator-controls.md`](simulator-controls.md) and [`route-receipts.md`](route-receipts.md): original observations remain fixed, the approved/driver-received route is adopted at the exact cursor and replay restores the same transition without teleporting prior samples. | Local simulator control plane and synthetic closure; no public navigation certification. |
| S04 infeasible closure | Accepted | [`closure-routing.md`](closure-routing.md) and `docs/evidence/closure-ui-2026-09-11/`: a closure covering a required stop returns an explicit unresolved exception, retains the old plan and exposes the blocked review. | Rectangle exclusions are declared operations evidence, not inferred live traffic or lane-level closure modeling. |
| S05 detention boundary | Accepted | [`visit-session-policy.md`](visit-session-policy.md) and PostgreSQL threshold tests: below, exactly at and above 120 free minutes produce stable whole-minute results under the bound contract; raw observed duration and rounding rule remain visible. | Configured FTL terms only; no universal LTL free-time default. |
| S06 dock exhausts eligibility | Accepted | [`dock-hos.md`](dock-hos.md), [`cloud-dock-hos.md`](cloud-dock-hos.md) and [`native-dock-hos.md`](native-dock-hos.md): the open visit continues through zero on-duty headroom, provisional detention advances to CAD 75, the exhausted driver is rejected and an eligible replacement receives a separately approved/accepted load. | Planning aid, not certified ELD or automatic roadside relief. Selected simulator checkpoints do not claim continuous cloud-native motion. |
| S07 boundary jitter | Accepted | [`visit-session-policy.md`](visit-session-policy.md): 24 accuracy-disk boundary observations retain one visit and one draft; source confidence remains inspectable. | A confidently wrong source can still be wrong; accuracy is source-reported. |
| S08 GPS gap | Accepted | `docs/evidence/tracking-gaps-2026-09-11/`: the trail visibly breaks across uncertain/missing intervals, unknown values stay unknown and billing timestamps remain observed samples rather than reconstructed physical crossings. | No unsupported interval reconstruction or certified boundary time. |
| S09 adjacent facilities | Accepted | [`geofence-identity.md`](geofence-identity.md): overlapping candidate stops hold a new arrival for review with both identities retained; no visit or invoice is created until a unique confident observation. | Same-load stops in local built web; cross-assignment facility reconciliation and native ambiguity presentation remain outside this proof. |
| S10 exit and return | Accepted | [`visit-session-policy.md`](visit-session-policy.md): the declared no-grace rule splits a confident exit/return into two visits and applies configured free time per observed visit while retaining source events. | Contractual merge/grace alternatives are not implemented. |
| S11 duplicate/out-of-order delivery | Accepted | [`visit-reconciliation.md`](visit-reconciliation.md) and [`retrospective-mileage.md`](retrospective-mileage.md): exact duplicates are idempotent; valid late observations converge visits, billing and occurrence-ordered mileage with the ordered baseline while original sources and approved history remain. | Equal-time conflicts and missing identity remain held for review. |
| S12 disconnect/restart | Accepted | [`simulator-persistence.md`](simulator-persistence.md), `docs/evidence/simulator-restart-2026-09-11.json` and telemetry-freshness evidence: an actual process restart restores paused, retries exact pending IDs, shows a quiet source and does not fabricate completion. | Local atomic disk persistence; disk loss, distributed writers and cloud container replacement are not proven. |
| S13 accelerated unequal ticks | Accepted | Optimizer/simulator transition tests and the replay receipts in [`simulator-slowdowns.md`](simulator-slowdowns.md): unequal advance batches reproduce the canonical one-second stream and odometer integral from simulated elapsed time. | Seeded motion model, not posted-speed or real-traffic calibration. |
| S14 HOS history/headroom | Accepted | `docs/evidence/hos-review-2026-09-11/` and `docs/evidence/hos-profile-2026-09-11.md`: missing/stale duty history is distinguished from Cycle 1/2 exhaustion; unsupported profiles do not receive an unqualified green state. | Declared federal south-of-60 solo profile; RoadStar is not a certified ELD. |
| S15 gross passes, group fails | Accepted | [`axle-assessment.md`](axle-assessment.md), [`cloud-axle-review.md`](cloud-axle-review.md) and [`native-axle-review.md`](native-axle-review.md): the 32,000 lb fixture passes configured gross while failing trailer group, blocks direct/batch/approval/acceptance paths, retains source evidence and passes only after a separately reviewed loading position. | Static supported model with configured limits; not Ontario legal clearance or physical measurement. |
| S16 infeasible attractive return | Accepted | [`return-load.md`](return-load.md): zero-deadhead London pickup is rejected because it precedes release; reachable Kitchener return remains, reports actual Valhalla/OR-Tools duty, preserves reservations and receives separate approval/acceptance. | Local synthetic appointments; no optimality, revenue or savings claim. |
| S17 multi-stop LTL | Accepted | `docs/evidence/cloud-ltl-optimization.json`, `docs/evidence/cloud-group-completed.png` and group-road/remaining-work PostgreSQL tests: capacity changes at ordered stops, overload is independently rejected, scoped completion advances only the correct shipment and FTL detention is not applied as a universal LTL rule. | Supported grouped fixture, not every consolidation policy or terminal-transfer network. |
| S18 competing approvals | Accepted | [`competing-approvals.md`](competing-approvals.md): two browser contexts submit overlapping shared-truck approvals together; one returns 200, one visibly returns 409 and PostgreSQL retains one assignment/approval plus one pending loser. | Controlled local concurrency, not a distributed contention benchmark. |
| S19 complete replay twice | Accepted | [`complete-replay.md`](complete-replay.md): 10,201 ordered observations reproduce the exact event hash and all 13 compared operational record sets remain byte-equivalent; visit identities and the approved CAD 75 revision do not double. | Same-run idempotent replay, not an independent counterfactual or savings comparison. |
| S20 conflicting manual evidence | Accepted | [`visit-time-review.md`](visit-time-review.md), [`native-visit-correction.md`](native-visit-correction.md) and hosted/native evidence: original GPS and prior CAD 75 approval remain immutable; a source-versioned correction records actor, reason and superseding values and creates a separate CAD 50 draft. | Identified closed visit with stored same-shipment source. Missing/ambiguous visit identity cannot be bypassed. |

## Cross-scenario evidence

- The latest configured backend/PostgreSQL/real-Valhalla regression at the competing-approval checkpoint passed 106 of 106 tests with zero skips. The later ordinary-trip packet is read-only.
- The independent simulator remains a separate process with its own clock and retained checkpoints. Operational mutations still pass through authenticated, idempotent API commands.
- The current hero path is server-ranked recovery → dispatcher approval → separate driver acceptance. The 3D dock and corridor views project acknowledged source events and issue no operational commands.
- The September 10 Firebase/Cloud Run/Cloud SQL burst remains the current representative 131-driver capacity evidence: 524 telemetry writes plus 524 synchronization pairs, zero recorded failures and 5,464 ms p95 acknowledgement-to-snapshot lag. It predates later migrations and is not relabeled as a current soak test.

## Remaining completion gates outside the scenario matrix

The scenario matrix is covered within the stated boundaries. The full product goal still has external and hardware gates: physical Android background-location verification, native iOS execution on Xcode/iPhone, human listening of the final film, two timed live rehearsals, organizer clarification, judge credential/private-repository delivery, authenticated portal inventory and the separate final submission action. The operator explicitly deferred physical phone testing.
