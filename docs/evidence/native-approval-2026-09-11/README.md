# Native recovery approval — September 11, 2026

Accepted packet: native dispatcher recovery evidence and approval parity. Main authorized; mobile-only source scope; 30-minute ceiling, two repairs per blocker, no new resources/model spend. Third visual direction preserved.

Before: Android omitted the modeled current/replacement outcomes and resolved the current driver from whichever assignment remained active. After approval that could incorrectly show the replacement in both positions. The screen now resolves the original assignment by the proposal's retained ID, shows both pickup/completion outcomes and rejected current constraints, identifies equipment and scenario assumptions, and distinguishes an offer from driver acceptance. Changed load/resource evidence disables approval; the API still rechecks atomically. Exact proposal revision and equipment appear in the native confirmation.

Verification used fresh synthetic carrier `consequences-native-approval-20260911`, existing seed execution `roadstar-film-seed-k8knf`. Its informational startup retry was observed to completion without restart. D-02 is explicitly staged in London. Scenario observation time is September 13 11am ET. Valhalla estimates current pickup 1:30pm (75 minutes late) versus replacement 12:15pm, completing 2:39pm. These are modeled assumptions, not measured savings or live ETAs.

The API35 release performed dispatcher review and approval; Android displayed awaiting acceptance. A separate authenticated hosted web session also observed the offered state. Signing into the replacement driver account on Android and pressing Accept trip produced accepted version2. The already-open web session synchronized to accepted without a manual reload. Cloud readback confirms one approved proposal, original assignment superseded, replacement accepted. Returning to native dispatcher confirmed D-01 Before / D-02 Approved and the accepted message. No API call substituted for either native approval or acceptance.

Root/native TypeScript pass, 34 native tests pass with zero skipped, release ARM64/x86_64 build and install pass. Tests cover preserved original assignment, offered/accepted/superseded results, stale/missing load/resource evidence and absent feasibility proof; existing queue tests retain stale approvals as failed. Built native comparison, confirmation, before/after and accepted screenshots plus hosted web capture were inspected directly. Other captures preserve intermediate states. Hosted web had zero page errors/overflow. APK hash and receipts are retained here.

The original native-current D02 accepted trip is restored after verification. Cloud API/web revisions unchanged; no push. Physical Android/iOS and provider push delivery remain deferred/unverified. Native proposal creation still starts from the web planning board. Next: remaining simulator acceptance scenarios, restart/replay persistence, measured workflow and demo alignment.
