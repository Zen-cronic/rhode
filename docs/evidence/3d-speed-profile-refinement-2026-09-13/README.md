# Matched 401 speed-profile refinement

The matched replay retains all 64 exact at-or-before observation pairs while replacing the visually dominant speed columns with thin, low pins. Pin height still varies with recorded speed; missing or route-incompatible observations remain absent rather than becoming zero-speed marks.

The inspected T+40:00 perspective capture keeps the route and two trucks visually dominant. The corresponding accessible diagram uses the same reduced height and thinner strokes. This is presentation refinement only: recording selection, comparison values, intervention timing, progress gap, modeled finish delta and operational read-only boundaries are unchanged.

Verification: complete web TypeScript/Vite build, four web model/auth tests and direct visual inspection of `local-t40-slim-pins.png`.
